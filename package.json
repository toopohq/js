{
  "name": "@toopo/js",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.5.1",
  "scripts": {
    "check": "biome ci --error-on-warnings && tsc && tsc -p tsconfig.baseline.json && vitest run && knip"
  },
  "knip": {
    "entry": [
      ".claude/hook.mjs"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@toopo/spec": "github:toopohq/spec",
    "@types/node": "24.13.6",
    "knip": "6.37.0",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
